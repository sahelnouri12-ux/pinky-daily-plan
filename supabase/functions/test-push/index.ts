// @ts-ignore npm package has no Deno-native types
import webpush from "npm:web-push@3.6.7";
import { json, preflight, requireUser, safeString } from "../_shared/server.ts";
function configure(){const pub=Deno.env.get("VAPID_PUBLIC_KEY");const priv=Deno.env.get("VAPID_PRIVATE_KEY");if(!pub||!priv)throw new Error("VAPID keys are missing.");webpush.setVapidDetails(Deno.env.get("VAPID_SUBJECT")||"mailto:admin@example.com",pub,priv);}
export default { async fetch(request: Request) { const p=preflight(request); if(p)return p; if(request.method!=="POST")return json({error:"Method not allowed"},405,{Allow:"POST"}); const auth=await requireUser(request); if("error" in auth)return auth.error; const body=await request.json().catch(()=>({})); const deviceId=safeString(body.deviceId,180); if(!deviceId)return json({error:"Device ID is required"},400); try{configure();}catch(error){return json({error:(error as Error).message},503);} const {data:row,error}=await auth.supabase.from("push_subscriptions").select("subscription,language").eq("user_id",auth.user.id).eq("device_id",deviceId).maybeSingle(); if(error)return json({error:error.message},500); if(!row)return json({error:"No push subscription for this device"},404); const language=body.language==="en"||row.language==="en"?"en":"fa"; try{await webpush.sendNotification(row.subscription,JSON.stringify({title:"Pinky Daily Plan ♡",body:language==="en"?"Phone notifications are working.":"اعلان گوشی با موفقیت فعال است.",tag:"pinky-test",url:"app.html#today"}),{TTL:60,urgency:"high"});return json({ok:true});}catch(pushError:any){if([404,410].includes(pushError.statusCode)){await auth.supabase.from("push_subscriptions").delete().eq("user_id",auth.user.id).eq("device_id",deviceId);}return json({error:pushError.message||"Push delivery failed"},502);} } };
